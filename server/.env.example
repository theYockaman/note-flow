PORT=3001
# REQUIRED: Set a strong, random secret key for JWT token generation
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
DB_PATH=./data/noteflow.db
NODE_ENV=development
